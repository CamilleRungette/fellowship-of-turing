console.log("connected")

const decide = () => {
    var form = document.getElementById("form-testing");
    form.classList.toggle("form-toggle");
    var main = document.getElementById("main-testing");
    main.classList.toggle("main-toggle");
};